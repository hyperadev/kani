apiVersion: v1
kind: Service
metadata:
  name: kani
  namespace: default
  labels:
    app: kani
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: kani